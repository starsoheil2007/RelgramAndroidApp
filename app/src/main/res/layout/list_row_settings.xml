<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="viewModel"
            type="com.relgram.app.app.view_models.SettingsListViewModel" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:background="@drawable/back_settings_row">

        <com.relgram.app.app.library.IconTextView
            android:id="@+id/icon"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            android:text="@{viewModel.icon}"
            android:gravity="center"
            android:textSize="@dimen/text_size_xxlarge"
            android:textColor="@{viewModel.iconColor}" />

        <com.relgram.app.app.library.PersianTextView
            android:id="@+id/title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_toLeftOf="@+id/icon"
            android:text="@{viewModel.title}" />

    </RelativeLayout>
</layout>
